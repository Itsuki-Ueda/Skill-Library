# INBOX — 記憶の候補箱

教訓・罠の初出はここに1行。同じ主題が再発したら回数+1（行は増やさない）。
回数3で `/memory-ops promote` により昇格先へ移す。初出から60日で回数3未満のままなら削除。
起動時には読まない。上限60行。

対処（回避 or 検出方法）が決まっていないものは入れない（Blockers / BACKLOG へ）。手順の全文はミッションファイルに残し、ここは1行。

| 初出 | 最終 | 回数 | 症状・条件 | 対処（回避 or 検出） | 根拠・詳細 | 昇格先 |
| --- | --- | --- | --- | --- | --- | --- |
